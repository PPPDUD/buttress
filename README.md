buttress
=============

Chromium extension that replaces occurrences of 'the cloud' with 'my butt'. Forked from the `cloud-to-butt` project and updated to support Manifest v3.

This version also fixes a small bug in upstream where it tries to modify <script> elements.
