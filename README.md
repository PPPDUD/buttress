buttress
=============

Chromium extension that replaces occurrences of 'the cloud' with 'my butt'. Buttress is a fork of the awesome `cloud-to-butt` extension, but it's actively-maintained. The goal is to keep all of the original intended functionality and none of the bugs.

This version also fixes a small bug in upstream where it tries to modify <script> elements.
